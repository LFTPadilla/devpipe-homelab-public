# Security Policy (Public Repo)

- Never commit real secrets, tokens, keys, or passwords.
- Use `.env.example` with placeholder values only.
- Keep infrastructure docs sanitized (no real public/private IPs, domains, usernames, or bucket names).
- Rotate any credential that was ever committed, even if removed later.
