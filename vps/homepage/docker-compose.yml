services:
  homepage:
    image: ghcr.io/gethomepage/homepage:latest
    container_name: homepage
    restart: unless-stopped
    ports:
      - 3003:3000
    volumes:
      - ./config:/app/config
    env_file:
      - .env
